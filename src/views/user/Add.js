import React, { Component, Fragment } from "react";


export default class AddA extends Component {
  constructor(prpos) {
    super(prpos);
    this.state = {};
  }

  render() {
    return <Fragment>用户添加</Fragment>;
  }
}
